# Production server. Copy to .env; keep .env out of Git.
# Leave these unset for an unauthenticated loopback-only local test of npm run serve.
# MELEE_PUBLIC_ORIGIN=https://melee.example.com
# MELEE_ACCESS_KEY=replace-with-a-random-password-of-at-least-24-characters
MELEE_BIND_HOST=127.0.0.1
MELEE_PORT=3000
