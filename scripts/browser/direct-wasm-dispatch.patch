--- a/vendor/dolphin/Source/Core/Core/CMakeLists.txt
+++ b/vendor/dolphin/Source/Core/Core/CMakeLists.txt
@@ -962,3 +962,12 @@
     "${DOLPHIN_WASM_JIT_CACHE_PRE_JS}"
   )
 endif()
+
+# Browser experiment: direct calls for generated WASM blocks, scoped to the
+# interpreter translation unit so the rest of the engine keeps its build flags.
+if(EMSCRIPTEN)
+  set_property(SOURCE PowerPC/CachedInterpreter/CachedInterpreter.cpp APPEND
+    PROPERTY COMPILE_OPTIONS
+      -UDOLPHIN_WEB_DIRECT_WASM_BLOCK_DISPATCH
+      -DDOLPHIN_WEB_DIRECT_WASM_BLOCK_DISPATCH=1)
+endif()
--- a/vendor/dolphin/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp
+++ b/vendor/dolphin/Source/Core/Core/PowerPC/CachedInterpreter/CachedInterpreter.cpp
@@ -11037,6 +11037,11 @@
   s_wasm_jit_requested_mode.store(std::clamp(enabled, 0, 2), std::memory_order_release);
 }
 
+extern "C" int GetPpcWasmDirectDispatch()
+{
+  return DOLPHIN_WEB_DIRECT_WASM_BLOCK_DISPATCH;
+}
+
 extern "C" int GetPpcWasmJitRequestedMode()
 {
   return s_wasm_jit_requested_mode.load(std::memory_order_acquire);
--- a/core/upstream/dolphin_web_core.cpp
+++ b/core/upstream/dolphin_web_core.cpp
@@ -59,6 +59,7 @@
 extern "C" std::uint32_t DolphinWeb_GetCachedInterpreterDisableMask();
 extern "C" void NotifyWebGpuOwnershipTraceLoadRequested();
 extern "C" int GetPpcWasmJitRequestedMode();
+extern "C" int GetPpcWasmDirectDispatch();
 extern "C" int GetPpcWasmJitAppliedMode();
 extern "C" std::uint32_t GetPpcWasmJitModeFlushes();
 
@@ -1148,6 +1149,7 @@
       << ",\"jitRequestedMode\":" << GetPpcWasmJitRequestedMode()
       << ",\"jitAppliedMode\":" << GetPpcWasmJitAppliedMode()
       << ",\"jitModeFlushes\":" << GetPpcWasmJitModeFlushes()
+      << ",\"directWasmDispatch\":" << GetPpcWasmDirectDispatch()
       << ",\"internalScale\":" << Config::Get(Config::GFX_EFB_SCALE)
       << ",\"scaledEfbCopies\":" << Config::Get(Config::GFX_HACK_COPY_EFB_SCALED)
       << ",\"cpuThread\":" << Config::Get(Config::MAIN_CPU_THREAD)
