--- /dev/null
+++ b/vendor/dolphin/Source/Core/Common/BrowserCopyBytes.h
@@ -0,0 +1,40 @@
+// Private snapshot copies occur with emulation paused and disjoint buffers.
+#pragma once
+#include <cstddef>
+#include <cstring>
+#ifdef __wasm_simd128__
+#include <wasm_simd128.h>
+#endif
+namespace DolphinWeb {
+inline void CopySnapshotBytes(void* destination, const void* source, std::size_t bytes)
+{
+#ifdef __wasm_simd128__
+  // Volatile packed vector accesses preserve explicit SIMD transfers rather
+  // than folding the loop back into shared-memory memory.copy.
+  struct __attribute__((packed, may_alias)) Vector { v128_t value; };
+  auto* out = static_cast<unsigned char*>(destination);
+  auto* in = static_cast<const unsigned char*>(source);
+  while (bytes >= 64)
+  {
+    const auto a = reinterpret_cast<const volatile Vector*>(in)->value;
+    const auto b = reinterpret_cast<const volatile Vector*>(in+16)->value;
+    const auto c = reinterpret_cast<const volatile Vector*>(in+32)->value;
+    const auto d = reinterpret_cast<const volatile Vector*>(in+48)->value;
+    reinterpret_cast<volatile Vector*>(out)->value = a;
+    reinterpret_cast<volatile Vector*>(out+16)->value = b;
+    reinterpret_cast<volatile Vector*>(out+32)->value = c;
+    reinterpret_cast<volatile Vector*>(out+48)->value = d;
+    in += 64; out += 64; bytes -= 64;
+  }
+  while (bytes >= 16)
+  {
+    reinterpret_cast<volatile Vector*>(out)->value =
+        reinterpret_cast<const volatile Vector*>(in)->value;
+    in += 16; out += 16; bytes -= 16;
+  }
+  while (bytes--) *out++ = *in++;
+#else
+  std::memcpy(destination, source, bytes);
+#endif
+}
+}
--- /dev/null
+++ b/vendor/dolphin/Source/Core/Common/BrowserStateCopy.h
@@ -0,0 +1,29 @@
+#pragma once
+#include <atomic>
+#include "Common/BrowserCopyBytes.h"
+#include "Common/ChunkFile.h"
+namespace DolphinWeb {
+inline std::atomic<bool> browser_state_transfer_active{false};
+struct BrowserStateTransferScope {
+  BrowserStateTransferScope() { browser_state_transfer_active.store(true, std::memory_order_release); }
+  ~BrowserStateTransferScope() { browser_state_transfer_active.store(false, std::memory_order_release); }
+};
+inline void TransferSnapshotMemory(PointerWrap& p, u8* data, u32 bytes)
+{
+  if (!browser_state_transfer_active.load(std::memory_order_acquire))
+  {
+    p.DoArray(data, bytes);
+    return;
+  }
+  // This length-prefixed form is private to browser in-memory checkpoints.
+  // Disk savestates keep Dolphin's original serialization format.
+  u32 stored_size = bytes;
+  u8* storage = p.DoExternal(stored_size);
+  if (p.IsReadMode())
+  {
+    if (stored_size != bytes) { p.SetVerifyMode(); return; }
+    CopySnapshotBytes(data, storage, bytes);
+  }
+  else if (p.IsWriteMode()) CopySnapshotBytes(storage, data, bytes);
+}
+}
--- a/vendor/dolphin/Source/Core/Core/HW/Memmap.cpp
+++ b/vendor/dolphin/Source/Core/Core/HW/Memmap.cpp
@@ -1,3 +1,4 @@
+#include "Common/BrowserStateCopy.h"
 // Copyright 2008 Dolphin Emulator Project
 // SPDX-License-Identifier: GPL-2.0-or-later
 
@@ -530,14 +531,14 @@
     return;
   }
 
-  p.DoArray(m_ram, current_ram_size);
-  p.DoArray(m_l1_cache, current_l1_cache_size);
+  DolphinWeb::TransferSnapshotMemory(p, m_ram, current_ram_size);
+  DolphinWeb::TransferSnapshotMemory(p, m_l1_cache, current_l1_cache_size);
   p.DoMarker("Memory RAM");
   if (current_have_fake_vmem)
-    p.DoArray(m_fake_vmem, current_fake_vmem_size);
+    DolphinWeb::TransferSnapshotMemory(p, m_fake_vmem, current_fake_vmem_size);
   p.DoMarker("Memory FakeVMEM");
   if (current_have_exram)
-    p.DoArray(m_exram, current_exram_size);
+    DolphinWeb::TransferSnapshotMemory(p, m_exram, current_exram_size);
   p.DoMarker("Memory EXRAM");
 }
 
--- a/vendor/dolphin/Source/Core/Core/HW/DSP.cpp
+++ b/vendor/dolphin/Source/Core/Core/HW/DSP.cpp
@@ -1,3 +1,4 @@
+#include "Common/BrowserStateCopy.h"
 // Copyright 2008 Dolphin Emulator Project
 // SPDX-License-Identifier: GPL-2.0-or-later
 
@@ -79,7 +80,7 @@
 void DSPManager::DoState(PointerWrap& p)
 {
   if (!m_aram.wii_mode)
-    p.DoArray(m_aram.ptr, m_aram.size);
+    DolphinWeb::TransferSnapshotMemory(p, m_aram.ptr, m_aram.size);
   p.Do(m_dsp_control);
   p.Do(m_audio_dma);
   p.Do(m_aram_dma);
--- a/vendor/dolphin/Source/Core/VideoCommon/Fifo.cpp
+++ b/vendor/dolphin/Source/Core/VideoCommon/Fifo.cpp
@@ -1,3 +1,4 @@
+#include "Common/BrowserStateCopy.h"
 // Copyright 2008 Dolphin Emulator Project
 // SPDX-License-Identifier: GPL-2.0-or-later
 
@@ -71,7 +72,7 @@
 
 void FifoManager::DoState(PointerWrap& p)
 {
-  p.DoArray(m_video_buffer, FIFO_SIZE);
+  DolphinWeb::TransferSnapshotMemory(p, m_video_buffer, FIFO_SIZE);
   u8* write_ptr = m_video_buffer_write_ptr;
   p.DoPointer(write_ptr, m_video_buffer);
   m_video_buffer_write_ptr = write_ptr;
--- a/vendor/dolphin/Source/Core/Core/State.cpp
+++ b/vendor/dolphin/Source/Core/Core/State.cpp
@@ -1,3 +1,4 @@
+#include "Common/BrowserStateCopy.h"
 // Copyright 2008 Dolphin Emulator Project
 // SPDX-License-Identifier: GPL-2.0-or-later
 
@@ -323,6 +324,7 @@
   browser_state_sizes[slot] = 0;
   browser_states_gpu[slot] = gpu_resident;
   const BrowserTextureStateScope texture_scope(slot, gpu_resident, true);
+  const DolphinWeb::BrowserStateTransferScope transfer_scope;
   constexpr std::size_t max_bytes = 128 * 1024 * 1024;
   constexpr std::size_t total_budget = 640 * 1024 * 1024;
   // Measure before allocating; never let an unexpected game/state consume
@@ -407,6 +409,7 @@
   // Source bytes and instruction-cache coherence must agree on both sides
   // of restore before compiled blocks may be retained.
   const BrowserLoadingScope loading_scope;
+  const DolphinWeb::BrowserStateTransferScope transfer_scope;
   CodeRestoreGuard code_guard(system);
   if (browser_states_gpu[slot] && !g_texture_cache) return false;
   const BrowserTextureStateScope texture_scope(slot, browser_states_gpu[slot], false);
