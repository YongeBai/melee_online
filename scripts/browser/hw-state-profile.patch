--- a/vendor/dolphin/Source/Core/Core/HW/HW.cpp
+++ b/vendor/dolphin/Source/Core/Core/HW/HW.cpp
@@ -87,26 +87,37 @@
 {
   system.GetMemory().DoState(p);
   p.DoMarker("Memory");
+  State::BrowserRecordSection(p, "HW Memory");
   system.GetMemoryInterface().DoState(p);
   p.DoMarker("MemoryInterface");
+  State::BrowserRecordSection(p, "HW MemoryInterface");
   system.GetVideoInterface().DoState(p);
   p.DoMarker("VideoInterface");
+  State::BrowserRecordSection(p, "HW VideoInterface");
   system.GetSerialInterface().DoState(p);
   p.DoMarker("SerialInterface");
+  State::BrowserRecordSection(p, "HW SerialInterface");
   system.GetProcessorInterface().DoState(p);
   p.DoMarker("ProcessorInterface");
+  State::BrowserRecordSection(p, "HW ProcessorInterface");
   system.GetDSP().DoState(p);
   p.DoMarker("DSP");
+  State::BrowserRecordSection(p, "HW DSP");
   system.GetDVDInterface().DoState(p);
   p.DoMarker("DVDInterface");
+  State::BrowserRecordSection(p, "HW DVDInterface");
   system.GetGPFifo().DoState(p);
   p.DoMarker("GPFifo");
+  State::BrowserRecordSection(p, "HW GPFifo");
   system.GetExpansionInterface().DoState(p);
   p.DoMarker("ExpansionInterface");
+  State::BrowserRecordSection(p, "HW ExpansionInterface");
   system.GetAudioInterface().DoState(p);
   p.DoMarker("AudioInterface");
+  State::BrowserRecordSection(p, "HW AudioInterface");
   system.GetHSP().DoState(p);
   p.DoMarker("HSP");
+  State::BrowserRecordSection(p, "HW HSP");
 
   if (system.IsWii())
   {
--- a/vendor/dolphin/Source/Core/Core/HW/Memmap.cpp
+++ b/vendor/dolphin/Source/Core/Core/HW/Memmap.cpp
@@ -1,3 +1,4 @@
+#include "Core/State.h"
 #include "Common/BrowserStateCopy.h"
 // Copyright 2008 Dolphin Emulator Project
 // SPDX-License-Identifier: GPL-2.0-or-later
@@ -534,12 +535,15 @@
   DolphinWeb::TransferSnapshotMemory(p, m_ram, current_ram_size);
   DolphinWeb::TransferSnapshotMemory(p, m_l1_cache, current_l1_cache_size);
   p.DoMarker("Memory RAM");
+  State::BrowserRecordSection(p, "HW Memory RAM");
   if (current_have_fake_vmem)
     DolphinWeb::TransferSnapshotMemory(p, m_fake_vmem, current_fake_vmem_size);
   p.DoMarker("Memory FakeVMEM");
+  State::BrowserRecordSection(p, "HW Memory FakeVMEM");
   if (current_have_exram)
     DolphinWeb::TransferSnapshotMemory(p, m_exram, current_exram_size);
   p.DoMarker("Memory EXRAM");
+  State::BrowserRecordSection(p, "HW Memory EXRAM");
 }
 
 void MemoryManager::Shutdown()
